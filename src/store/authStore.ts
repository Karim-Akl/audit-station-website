// import { create } from "zustand";

// type Store = {
//   email: string;
//   userId: string;
//   setUserData: ({ email, userId }: { email: string; userId: string }) => void;
// };

// export const useAuthStore = create<Store>((set) => ({
//   email: "",
//   userId: "",
//   setUserData: ({ email, userId }) => set({ email, userId }),
// }));
